import { extendTheme } from "@chakra-ui/react";
import { styles, components, config, colors } from "./styles";

const theme = extendTheme({ styles, components, colors, config });
export default theme;
